# --------------------------------------------------------------------------------
#           database
# --------------------------------------------------------------------------------
# falls die IP sich geändert hat, startet Quarkus nicht
# docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' mariadb-all
QUARKUS_DATASOURCE_JDBC_URL=jdbc:mariadb://172.18.0.2:3306/minikaenguru
QUARKUS_DATASOURCE_PASSWORD=hwinkel

# --------------------------------------------------------------------------------
#           logging
# --------------------------------------------------------------------------------
QUARKUS_LOG_FILE_ENABLED=false
QUARKUS_HTTP_ACCESS_LOG_LOG_TO_FILE=false

QUARKUS_HTTP_TEST_TIMEOUT=120s
