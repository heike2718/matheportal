{
    "name": "shared-runtime-config",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/shared-runtime-config/src",
    "prefix": "mp",
    "projectType": "library",
    "tags": ["domain:shared", "type:config"],
    "targets": {
        "test": {
            "executor": "@nx/vitest:test",
            "outputs": ["{options.reportsDirectory}"],
            "options": {
                "reportsDirectory": "coverage/libs/shared-runtime-config"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        }
    }
}
